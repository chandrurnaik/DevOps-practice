apiVersion: batch/v1
kind: Job
metadata:
  name: my-job
spec:
  completions: 5
  template:
    spec:
      containers:
        - name: busybox
          image: busybox
          command:
          - /bin/sh
          - -c
          - "sleep 2; exit"
      restartPolicy: Never